namespace Cognizant.Common.Constants
{
    public static class FirebaseConstants
    {
        // All events should be 40 char max.

        public const string TIME_IN_VIEW = nameof(TIME_IN_VIEW);

        public static string APP_INSTALLED =nameof(APP_INSTALLED);
        public static string APP_OPENED =nameof(APP_OPENED);
        public static string APP_FIRST_TIME_OPENED = nameof(APP_FIRST_TIME_OPENED);
        public static string APP_UPDATED = nameof(APP_UPDATED);
    }
}
